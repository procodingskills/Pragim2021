({
    doInitHandler : function(component, event, helper) {
        const startIndex = component.get("v.startIndex");
        const componentName = component.get("v.componentList")[startIndex];
    }
})
